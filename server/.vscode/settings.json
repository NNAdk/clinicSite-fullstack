{
    // Глобальне налаштування для всього проєкту (якщо потрібно)
    "files.eol": "auto", 

    // Налаштування, яке ПЕРЕКРИВАЄ попереднє лише для папки 'server/**'
    "[**/*.ts]": { // Застосовуємо для всіх файлів TypeScript (основний для NestJS)
        "files.eol": "\n" // Встановлює LF
    },
    "[**/*.js]": { // Застосовуємо для всіх файлів JavaScript
        "files.eol": "\n" // Встановлює LF
    }
}